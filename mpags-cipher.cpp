#include <iostream>
#include <string>
int main()
{
  char in_char{'x'};
  std::cout<<"Type the input; press ctrl+d when finished"<< "\n";
  std::
  while(std::cin>>in_char)
    {switch(in_char)
      case 0:
	std::string

      putchar (toupper(in_char));
	}
  return 0;
      
}
